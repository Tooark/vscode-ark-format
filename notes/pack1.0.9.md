# Release pack1.0.9

## Alterações desta versão

- Bump de versão do pacote `ark-format`: `1.0.8` -> `1.0.9`.
- Alinhamento do extension pack com a rodada de manutenção de segurança dos pacotes de formatação:
  - `ark-format-shell` com atualização de dependências vulneráveis (avisos do Dependabot).
  - `ark-format-powershell` com atualização de dependências vulneráveis (avisos do Dependabot).
- Atualização das dependências de build/test do workspace: `esbuild` (`0.28.1`), `vite` (`>=8.0.16`), `undici` (`>=7.28.0`), `markdown-it` (`>=14.2.0`), `tmp` (`>=0.2.7`) e `form-data` (`>=4.0.6`).
- Sem alterações de configuração do extension pack além do alinhamento de release.

## Informações Adicionais

- Tag relacionada: `pack1.0.9`
- Data de lançamento: **2026-06-27**
